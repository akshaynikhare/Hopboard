apiVersion: v1
kind: Service
metadata:
  name: {{ include "hopboard-relay.fullname" . }}
  labels:
    {{- include "hopboard-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hopboard-relay.selectorLabels" . | nindent 4 }}
